const getTotalCost = (sides, noOfLayer, perVundleSizd,perBuncleCost)=> {
    parameter = 0;
    sides.forEach((Element)=>{
        parameter=parameter + Element;
    })
    console.log(((parameter)))
}